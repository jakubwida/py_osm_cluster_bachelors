
#this file is currently unused


def avg_coords(coords):
	x,y=list(zip(*coords))
	x=sum(x)
	y=sum(y)
	x=x/len(coords)
	y=y/len(coords)
	return(x,y)

#unfinished
def stdev_coords(coords):pass
#avg = avg_coords(coords)
#x,y =
